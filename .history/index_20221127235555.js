// code your solution here

function superbowlWin(records) {
    records.find(function (record) {
        if (record.result === "W"){
            return record.
        }

    });

    function win () {
        records.
    }
}
